# قبول المراجعة البصرية لـStage 3E.3

## نطاق الحزمة

تتطلب المراجعة 20 حالة مستقرة عند كل من `1920×1080` و`2560×1080`، بإجمالي 40 PNG على الأقل.

## الفحوص

- الأبعاد تطابق viewport لكل ملف.
- SHA-256 فريد داخل كل دقة، مع حالة دلالية مختلفة لكل لقطة.
- أعلى التطبيق يبدأ عند `y = 0` ولا توجد شرائط سوداء مصطنعة.
- لا تظهر حالات loading أو `جاري تجهيز المشهد`.
- اتجاه التطبيق RTL، والقيم الإنجليزية والمعرفات LTR.
- الفرق البصري بين Experience وCommand وStory يتجاوز ضوضاء الرسم الصغيرة.
- لا تظهر أسرار أو ادعاءات جاهزية أو Crowd values أو Route geometry.
- معاينة الخطة المفقودة حالة مقصودة، وليست صورة مكسورة.
- يمر `unzip -t` وتُسجل بصمة ZIP النهائية.

## اختبار الخمس ثوان

يمر بصريًا فقط عندما تظهر هوية الفعالية والتاريخ والحالة المرشحة والنقاط الخمس وتسلسل الرحلة وحالة المخطط والوضع النشط من الشاشة الأولى، من دون فتح مختبر التأليف.

## نتيجة التنفيذ

- `40` ملف PNG: عشرون حالة عند كل دقة.
- كل الأبعاد صحيحة وكل بصمات PNG الأربعين فريدة.
- الفرق المادي بين الأوضاع تجاوز حد `8%` المستخدم في الاختبار.
- لا تظهر حالات loading أو شرائط سوداء أو قص غير مقصود في الحزمة النهائية.
- نجح `unzip -t`.
- مسار ZIP: `/Users/mayadeen/Downloads/mayadeen-stage-3e3-experience-intelligence-review.zip`.
- SHA-256: `179149e7e2c7fb8f411ced48ec2f3444aea3fa5dc263bd06148eeb49c332ca91`.
